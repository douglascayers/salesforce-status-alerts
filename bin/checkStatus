#!/usr/bin/env node

var request = require('request');

var options = {
    url : 'https://salesforce-status-alerts.herokuapp.com/'
};

request( options, function( error, response, body ) {

    console.log( error );
    console.log( body );

}